# Apollo Windows Beta 1 Build 93 revision 3

This prerelease is the refreshed Windows owner package for Build 93.

- The extracted package now includes `Open Apollo Local UI.url` beside
  `Start Apollo Passive Receive.cmd`.
- The shortcut opens the local setup and dashboard page on the same Windows
  computer after Apollo starts.
- The page header shows both the release version and Build number.
- Settings includes a visible **Back to dashboard** control.
- Receiver-location guidance explains the protected-coordinate privacy boundary.

The existing Build 93 release and its historical asset remain unchanged.
